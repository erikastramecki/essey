# --- build artifacts ---
**/build/
**/target/
**/dist/
**/node_modules/
*.so

# --- secrets & keys (NEVER commit) ---
.env
.env.*
*.key
.operator-sui.key
.operator.json
*.keypair.json
.keys.json
**/.vercel/

# --- generated / local ---
src/docs.generated.ts
# Move ephemeral publication metadata (self-labeled "do not commit")
**/Pub.*.toml
.DS_Store

# --- foundry (rh-chain) ---
# Dependencies are reproduced by `forge install`, not vendored — see rh-chain/README.md
lib/
out/
cache/
broadcast/

# portable-proof demo cached keys (large, regenerated)
circuit/poseidon/.demo-keys/

# PFP builder — local art + taxonomy (private, pre-mint)
app/web/public/traits/
app/web/public/builder/
app/web/.reservations.json
.vercel
app/web/public/allowlist/
